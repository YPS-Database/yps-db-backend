# API

Desired endpoints:

```
/api/
    auth [POST]
    browsefields [GET]
    db [PUT]
    dbs [GET]
    page/{id} [GET,PUT]
    entry/{id} [GET]
    pages [GET] *
    ping [GET]
    search [GET]
```

Note, endpoints marked with `*` are optional extras.
